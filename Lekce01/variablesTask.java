package Lekce01;

import java.util.Date;

/**
 * V metodě main vytvoř následující proměnné:
 *
 *     Jméno klienta — budeme ukládat jména a příjmení klientů
 *
 *     Počet nákupů — kolik zákazníků si koupilo tento výrobek
 *
 *     Název města, kde firma sídlí.
 *
 *     Spotřeba vozidla v litrech na 100 km.
 *
 *     Množství kabelů — délka UTP kabelu na skladě v metrech.
 *
 *     Datum narození klienta.
 *
 *     Registrační značka vozidla.
 *
 *     Rodné číslo klienta.
 */

public class variablesTask {
    String clientName;
    int countOfPurchases;
    String cityName;
    double vehicleConsumptionInLittersPer100km;
    double cableLength;
    Date dateOfBirth;
    String dateOfBirth2;
    String vehicleRegistrationNumber;
    String personalIdentificationNumber;//with slash in it, e.g. 815612/0123
    long personalIdentificationNumber2;//without slash in it, e.g. 8156120123
}
